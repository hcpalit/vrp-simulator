import React from 'react'
import ReactDOM from 'react-dom/client'
import VRPSimulator from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VRPSimulator />
  </React.StrictMode>,
)
